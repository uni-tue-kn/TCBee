[package]
name = "ts_storage"
version = "0.1.0"
edition = "2021"
license = "MIT"

[features]
bundled = ["duckdb/bundled"]

[dependencies]
duckdb = { version = "1.3.2" }
ouroboros = "0.18.5"
sqlite = { version = "0.36.1" }
thiserror = "1.0.60"
